services:
  - type: web
    name: sistema-edge-producao
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    disk:
      name: edge-data
      mountPath: /var/data
      sizeGB: 10
